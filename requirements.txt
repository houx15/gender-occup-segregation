# Core dependencies for Gender-Occupation Segregation Analysis

# Word embeddings
gensim>=4.3.0

# Numerical computing
numpy>=1.24.0

# Data manipulation
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Configuration
PyYAML>=6.0

# CLI
fire>=0.5.0

# Web scraping and downloading
requests>=2.31.0

# Chinese text segmentation (for renminribao/weibo/newspaper)
jieba>=0.42.1

# English tokenization and stopwords (for ngram_en/coha pipelines)
nltk>=3.8.0

# Statistics
scipy>=1.10.0

# Choropleth maps (US state arm + China provincial arm). geopandas pulls in
# GDAL/Fiona/Shapely — on a conda env prefer `conda install -c conda-forge
# geopandas`, which resolves the native libs reliably; pip alone often can't.
geopandas>=0.13.0

# Testing
pytest>=7.4.0

# Optional: Jupyter for exploratory analysis
# jupyter>=1.0.0
# ipykernel>=6.25.0

# Optional: faster jieba (for weibo processing on cluster)
# jieba_fast>=0.53

# Optional: parquet support for weibo data
# fastparquet>=2023.1.0
